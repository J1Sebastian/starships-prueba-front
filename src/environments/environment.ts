/**
 * Se define la url base de la api, la cual se usara en los servicios para hacer las peticiones http.
 */

const baseUrl = 'https://swapi.py4e.com/api/';

export const environment = {
  production: false,
  baseUrl,
};
